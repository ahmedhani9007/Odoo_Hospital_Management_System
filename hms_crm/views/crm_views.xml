<odoo>
    <data>
        <!-- Inherit the CRM Partner Form View -->
        <record id="crm_partner_form_inherit" model="ir.ui.view">
            <field name="name">CRM Partner Form Inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <!-- Find the Sales and Purchase Tab and add the related_patient_id field inside the Miscellaneous group -->
                <xpath expr="//page[2]" position="inside">
                    <group>
                        <field name="related_patient_id"/>
                    </group>
                </xpath>
            </field>
        </record>

        <!-- Show website field in the list view for customers  -->
        <record id="view_res_partner_tree" model="ir.ui.view">
            <field name="name">res.partner.tree.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="inside">
                    <field name="website"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>


        <!-- Modify the Customer Form View -->
        <!-- <record id="view_partner_form" model="ir.ui.view">
            <field name="name">res.partner.form</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <form string="Customer">
                    <sheet>
                        <notebook>
                            <page string="Sales and Purchase">
                                <group string="Miscellaneous">
                                    <field name="related_patient_id"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record> -->